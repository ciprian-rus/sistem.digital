---
"@sistem-digital/react": minor
---

Add `@sistem-digital/react`: a hook per progressive enhancement in `@sistem-digital/components` (`useAccordion`, `useAutocomplete`, `useCharacterCount`, `useCookieBanner`, `useDialog`, `useDropdown`, `useErrorSummary`, `useExitThisPage`, `useFileUploadAdvanced`, `useTabs`, `useTooltip`), plus `useSistemDigitalEnhancements()`/`GlobalEnhancements` for page-wide enhancement in a single call. `apps/website` now consumes `GlobalEnhancements` instead of wiring the same four functions by hand.
