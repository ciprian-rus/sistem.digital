---
"@sistem-digital/components": minor
---

Add `bar-chart`: a horizontal bar chart rendered from a real `<table>` via CSS only (a `--sd-chart-value` custom property per row), not a separate visualization with a fallback table to keep in sync. No JavaScript. The value is always shown as text next to the bar, never conveyed by bar length alone.
